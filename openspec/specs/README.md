# Source-of-truth specs land here, organized by domain.
# Each domain folder (e.g. `solicitudes/`, `auth/`, `localAuth/`, `db/`) contains a `spec.md`
# that is the accumulated, merged result of all delta specs from `openspec/changes/*/specs/`.
# See ../_shared/openspec-convention.md for the merge rules.
